# Django Debug Mode (1 = enabled, 0 = disabled)
# WARNING: Never enable DEBUG in production!
DEBUG=1

# Django Secret Key
# SECURITY: Generate a new random secret key for each environment
# Use: python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'
SECRET_KEY=change-me-to-a-random-secret-key

# Allowed hosts for Django (space-separated list)
DJANGO_ALLOWED_HOSTS=localhost 127.0.0.1 [::1] backend

# Auth0 Configuration
# Get these values from your Auth0 Dashboard → Applications → Your App
# Leave blank - these should be set per developer or use shared tenant
AUTH0_DOMAIN=
AUTH0_CLIENT_ID=
AUTH0_AUDIENCE=

# PostgreSQL Database Configuration
SQL_ENGINE=django.db.backends.postgresql
SQL_DATABASE=capstone_projects_dev
SQL_USER=user123
SQL_PASSWORD=user123
SQL_HOST=db
SQL_PORT=5432
DATABASE=postgres
